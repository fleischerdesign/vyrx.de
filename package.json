{
  "name": "vyrx",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && node scripts/verify-contrast.mjs && node scripts/verify-content.mjs && node scripts/verify-adapters.mjs && npm run test",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test \"test/**/*.test.ts\"",
    "start": "node ./dist/server/entry.mjs"
  },
  "dependencies": {
    "@astrojs/node": "^11.1.6",
    "@fontsource-variable/dm-sans": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "astro": "^7.3.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.3"
  }
}
