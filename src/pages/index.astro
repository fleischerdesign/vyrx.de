---
import StartPage from '@/components/pages/StartPage.astro';

// Diese Seite liest die Identität aus den Kopfzeilen: sie ist serverseitig, nie vorgerendert.
export const prerender = false;
---

<StartPage locale="de" />
